BENCHMARKS + SALES LIBRARY LINK — done for you
===============================================

No hand edits this time. Both files are complete and ready.

    src/App.jsx        REPLACES yours — the three edits are already in
    src/Benchmarks.jsx REPLACES yours — adds the Tool Kit panel


UPLOAD
------
Add file -> Upload files, drag in the "src" folder, commit once.
Two files staged, both asking to overwrite. Both correct.

Paths must read:

    src/App.jsx
    src/Benchmarks.jsx


WHAT I CHANGED IN App.jsx
--------------------------
Your uploaded copy had no BenchmarkPanel in it at all, which is why
nothing was showing anywhere. Three changes, nothing else touched:

1. Line 2 — the import:
       import BenchmarkPanel from "./Benchmarks.jsx";

2. Line ~1059 — the empty-cell branch. Report() returned early for
   cells with no data, so the Sales Tool Kit cells never reached the
   panel. The early return is now wrapped and includes it.

3. Line ~1091 — the panel on cells that do have data, sitting under
   the KPIs and above the weekly trend.

Everything else in your file is untouched. Your CAMPAIGN_RULES, SEED
data, CSS and layout are exactly as you uploaded them.


WHAT YOU SHOULD SEE
-------------------
Click Disposition / Google Ads: under the KPIs, a "How this compares"
panel. CTR 1.52% below benchmark, cost per click $0.77 ahead of it,
cost per lead $58 in range, ROAS 3.6x in range.

Click any cell in the Sales Tool Kit row: a "Where the material lives"
panel with a link to the Sales Library and the tag to filter to.

Both verified in a browser against your actual file before packaging.


TAG MAPPING, GRID -> LIBRARY
-----------------------------
    reLink Ready    -> reLink Ready
    Transactional   -> Transactional
    reLink360       -> Consignment
    Net New         -> Net New

    Disposition     -> not mapped
    MEV             -> not mapped
    Brand           -> not mapped

Unmapped ones still get the link, without a tag hint. Tell me what
those three should point at and it is one line each — TOOLKIT_TAG near
the top of Benchmarks.jsx.


ALSO
----
READ-ME-FIRST.txt is sitting in your repo root again from the last
folder drag. Harmless, delete when convenient.
